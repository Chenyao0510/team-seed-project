fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
google-genai>=0.3
python-dotenv>=1.0
httpx>=0.27
opencv-python>=4.10
numpy>=2.0
# T52: 背景透過は rembg (U^2-Net 系) に置き換え。color-distance ヒューリスティックでは
# 足の間や髪の細部、AI 生成の単色背景の微妙な色ぶれを完璧に扱えなかったため、
# 人物セグメンテーションに振り切る。`[cpu]` で onnxruntime-cpu を確保 (GPU 不要)。
rembg[cpu]>=2.0

# Dev / test
pytest>=8.3
pytest-asyncio>=0.24
ruff>=0.7
